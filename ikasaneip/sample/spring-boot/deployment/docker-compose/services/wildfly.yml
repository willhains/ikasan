version: '2'

services:
  wildfly:
    image: ikasan/wildfly:latest
    container_name: wildfly
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "2"
