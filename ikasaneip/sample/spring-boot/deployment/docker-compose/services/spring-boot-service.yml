version: '2'

services:
  springboot-service:
    image: ikasan/springboot-service:latest
    restart: always
    ports:
      - "18080:18080"
    logging:
      driver: json-file
      options:
        max-size: "100m"
        max-file: "2"
